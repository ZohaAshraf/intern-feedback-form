# intern-feedback-form
A responsive feedback form for interns, built with vanilla HTML/CSS/JS. Multi-step wizard UI, client-side validation, and a live POST/GET integration with a mock REST API (JSONPlaceholder)
